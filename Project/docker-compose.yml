services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - APP_NAME=FastAPI App
      - VERSION=0.1.0
      - ENVIRONMENT=production
